on:
  pull_request:
    branches:
      - main

jobs:
  test:
    steps:
      - name: Print environment variables
        run: |
          echo "${{ runner.os }}"
          echo "${{ runner.arch }}"
