on:
  pull_request:
    branches:
      - main

jobs:
  runs-on: ubuntu-latest
  test:
    steps:
      - name: Print environment variables
        run: |
          echo "${{ runner.os }}"
          echo "${{ runner.arch }}"
